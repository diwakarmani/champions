package com.propertyapp.enums;

public enum NotificationType {
    PROPERTY_VIEWED,
    CONTACT_REVEALED,
    INQUIRY_RECEIVED,
    INQUIRY_RATING_PROMPT,   // sent to buyer when their inquiry auto-transitions to CONTACTED
    LISTING_APPROVED,
    LISTING_REJECTED
}
